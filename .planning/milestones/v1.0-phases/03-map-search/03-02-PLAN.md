---
phase: 03-map-search
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/components/map-view.tsx
  - src/app/search/page.tsx
  - src/components/slip-card.tsx
autonomous: false
requirements: [SRCH-01, SRCH-02]

must_haves:
  truths:
    - "Hovering a slip card in the list highlights the corresponding pin on the map"
    - "Hovering a pin on the map highlights/scrolls to the corresponding card in the list"
    - "On mobile, the page shows a slip list first with a floating Show Map button"
    - "Tapping Show Map opens a full-screen map overlay on mobile"
    - "The map centers on user's geolocation if permission is granted, otherwise South Florida"
  artifacts:
    - path: "src/components/map-view.tsx"
      provides: "Map with hover-highlighted pins and geolocation support"
      min_lines: 60
    - path: "src/app/search/page.tsx"
      provides: "Responsive layout with mobile map toggle"
      min_lines: 80
    - path: "src/components/slip-card.tsx"
      provides: "SlipCard with hover callback and highlight state"
      exports: ["default (SlipCard)"]
  key_links:
    - from: "src/components/slip-card.tsx"
      to: "src/app/search/page.tsx"
      via: "onHover and isHighlighted props"
      pattern: "onHover|isHighlighted"
    - from: "src/app/search/page.tsx"
      to: "src/components/map-view.tsx"
      via: "hoveredMarinaId state shared between map and list"
      pattern: "hoveredMarinaId"
---

<objective>
Add bi-directional hover sync between map pins and slip cards, responsive mobile layout with a "Show Map" toggle, and browser geolocation for initial map center.

Purpose: Delivers the polished, Airbnb-like interaction quality specified in user decisions — hover sync creates a connected experience, mobile layout makes it usable on phones, and geolocation personalizes the initial view.
Output: Interactive, responsive map search that feels premium on both desktop and mobile.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-map-search/03-CONTEXT.md
@.planning/phases/03-map-search/03-RESEARCH.md
@.planning/phases/03-map-search/03-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 output — executor reads the SUMMARY for exact signatures -->

From src/components/map-view.tsx (created in Plan 01):
```typescript
interface MapViewProps {
  marinas: Marina[];
  onVisibleMarinaIdsChange: (ids: Set<string>) => void;
  hoveredMarinaId: string | null;
  onHoverMarina: (id: string | null) => void;
  onSelectMarina: (id: string) => void;
}
export default function MapView(props: MapViewProps): JSX.Element;
```

From src/components/slip-card.tsx (existing):
```typescript
interface SlipCardProps {
  slip: Slip;
  checkIn?: string;
  checkOut?: string;
}
// Will be extended with: isHighlighted?: boolean, onHover?: (marinaId: string | null) => void
```

From src/app/search/page.tsx (rewritten in Plan 01):
```typescript
// State includes: hoveredMarinaId, visibleMarinaIds
// MapView is dynamically imported with ssr: false
// List renders SlipCards filtered by visibleMarinaIds
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bi-directional hover sync and geolocation</name>
  <files>src/components/slip-card.tsx, src/components/map-view.tsx, src/app/search/page.tsx</files>
  <action>
    1. Update `src/components/slip-card.tsx`:
       - Add optional props to SlipCardProps: `isHighlighted?: boolean`, `onHover?: (marinaId: string | null) => void`
       - Add `data-marina-id={slip.marinas.id}` attribute on the root div for scroll-into-view targeting
       - Add onMouseEnter={() => onHover?.(slip.marinas.id)) and onMouseLeave={() => onHover?.(null)) on the root div
       - When isHighlighted is true, add ring-2 ring-teal-500 shadow-lg classes to the root div (visual highlight)
       - These props are optional so existing SlipCard usage (outside search page) is unaffected

    2. Update `src/components/map-view.tsx`:
       - Add new prop: `initialCenter?: { longitude: number; latitude: number }` (optional, for geolocation result)
       - Use initialCenter in initialViewState if provided, otherwise default to South Florida (-80.1, 26.1)
       - Pin highlight: when hoveredMarinaId matches a marker, apply transform scale(1.3) and change pin color from teal-600 to navy-800. Use CSS transition for smooth effect.

    3. Update `src/app/search/page.tsx`:
       - Add geolocation useEffect: on mount, call navigator.geolocation.getCurrentPosition. On success, set initialCenter state to { longitude: pos.coords.longitude, latitude: pos.coords.latitude }. On error/deny, leave as undefined (MapView defaults to South Florida).
       - Pass initialCenter to MapView component
       - Pass isHighlighted={hoveredMarinaId === slip.marinas.id} and onHover={setHoveredMarinaId} to each SlipCard
       - Add scroll-into-view when hoveredMarinaId changes from map hover: useEffect that finds `[data-marina-id="${hoveredMarinaId}"]` and calls scrollIntoView({ behavior: 'smooth', block: 'nearest' }) on the list container. Only trigger when hover originates from map (track hover source with a ref to distinguish map-initiated vs card-initiated hovers).

    4. Key details:
       - Hover sync is bi-directional: card hover -> pin highlight, pin hover -> card highlight + scroll
       - Use a `hoverSource` ref ('map' | 'list' | null) to prevent scroll-into-view when user is hovering the list (would fight their scroll position)
       - Geolocation runs once on mount; no loading state needed (map renders immediately with default, then flyTo if geolocation succeeds after mount)
       - If geolocation succeeds after map has rendered, use mapRef.current.flyTo({ center: [lng, lat], zoom: 12 }) instead of re-setting initialViewState
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Hovering a SlipCard highlights corresponding map pin. Hovering a map pin highlights and scrolls to corresponding SlipCard. Map centers on user geolocation if permitted. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Mobile responsive layout with Show Map toggle and empty state overlay</name>
  <files>src/app/search/page.tsx, src/components/map-view.tsx</files>
  <action>
    1. Update `src/app/search/page.tsx` mobile layout:
       - Add `showMobileMap` boolean state (default false)
       - Desktop (md+ breakpoint): keep existing side-by-side layout (map 60%, list 40%)
       - Mobile (below md): show only the list panel by default (map hidden). Add a floating "Show Map" button fixed at bottom center: `fixed bottom-6 left-1/2 -translate-x-1/2 z-30 bg-navy-800 text-white px-6 py-3 rounded-full shadow-lg font-semibold flex items-center gap-2`. Include a small map icon. Button text: "Show Map".
       - When showMobileMap is true: render map as a fixed full-screen overlay (z-40, inset-0). Add an "X" close button (absolute top-4 right-4 z-50) and a "Show List" button at bottom center. Closing returns to list view.
       - The filters bar appears above the list on mobile (same as desktop, inside the list panel)

    2. Add empty state overlay on map:
       - When no marinas match the current filters (slips array is empty after Supabase fetch): show a semi-transparent overlay centered on the map with message "No marinas match your filters. Try adjusting vessel dimensions." Use bg-white/80 backdrop-blur-sm rounded-xl p-6 positioned absolute center of map container.
       - The map itself remains visible and interactive behind the overlay
       - When filters produce results but viewport shows no pins: list panel shows EmptyState component (already handled in Plan 01)

    3. Key details:
       - Use Tailwind responsive classes (hidden md:block, md:hidden) for show/hide — no JS media query needed for the base layout
       - The mobile map overlay should still have the filter bar accessible (render it above the map or as a collapsible drawer)
       - showMobileMap state resets to false when navigating away (no persistence needed)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>On mobile viewport: list shows first, "Show Map" button opens full-screen map overlay. On desktop: split layout unchanged. Empty state overlay appears on map when no marinas match filters. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete map search experience</name>
  <files>src/app/search/page.tsx</files>
  <action>
    Human verifies the complete map search experience built in Tasks 1-2 and Plan 01. Start the dev server with `npm run dev` and follow the verification steps below.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>User confirms: map renders with pins, hover sync works both directions, filters narrow results, mobile layout toggles correctly, empty states display properly, all without requiring login. User types "approved".</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npx vitest run` — all tests pass (including Phase 1 and 2 tests)
3. Full interaction test: browse map, hover sync, filter, mobile toggle — all work without auth

Human verification steps:
1. Open http://localhost:3000/search in an incognito/private window (not logged in)
2. Verify: Map renders with marina pins on the left, slip list on the right
3. Pan the map — verify list updates to show only visible marinas' slips
4. Hover a slip card — verify the corresponding map pin highlights (scale + color change)
5. Hover a map pin — verify the corresponding slip card highlights and scrolls into view
6. Enter a boat length (e.g., 30) and beam (e.g., 12) — click Search — verify list filters to compatible slips only
7. Click "Book Now" on a slip card — verify it redirects to login (not accessible without auth)
8. Resize browser to mobile width — verify list shows first with a "Show Map" floating button
9. Tap "Show Map" — verify full-screen map overlay appears with close button
10. Enter extreme filter values (length 999, beam 999) — verify empty state overlay appears on map
</verification>

<success_criteria>
- Hover sync works in both directions: card-to-pin and pin-to-card (SRCH-01 polish)
- Mobile layout shows list-first with Show Map toggle (SRCH-02 mobile UX)
- Map centers on geolocation when permitted, falls back to South Florida
- Empty state overlay on map when no results match filters
- Build and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-map-search/03-02-SUMMARY.md`
</output>
