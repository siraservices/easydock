---
phase: 02-landing-page-and-marina-onboarding
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - database/003_marina_leads.sql
  - src/app/api/leads/route.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/components/lead-modal.tsx
autonomous: true
requirements: [LAND-01, LAND-02]

must_haves:
  truths:
    - "Visiting the root URL renders the marketing landing page with Hero and How It Works sections"
    - "The lead capture form opens in a modal with name, email, and user type fields"
    - "Submitting the lead form with valid data creates a row in marina_leads and shows success feedback"
    - "Submitting with missing or invalid fields shows inline validation errors without submitting"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page with Hero, How It Works, and lead capture CTA"
      min_lines: 80
    - path: "src/components/lead-modal.tsx"
      provides: "Modal with lead capture form"
      min_lines: 60
    - path: "src/app/api/leads/route.ts"
      provides: "POST handler for marina_leads insert"
      exports: ["POST"]
    - path: "database/003_marina_leads.sql"
      provides: "marina_leads table DDL"
      contains: "CREATE TABLE marina_leads"
  key_links:
    - from: "src/components/lead-modal.tsx"
      to: "/api/leads"
      via: "fetch POST on form submit"
      pattern: "fetch.*api/leads"
    - from: "src/app/page.tsx"
      to: "src/components/lead-modal.tsx"
      via: "import and render with open/close state"
      pattern: "LeadModal"
    - from: "src/app/api/leads/route.ts"
      to: "supabase admin client"
      via: "createAdminClient().from('marina_leads').insert()"
      pattern: "createAdminClient"
---

<objective>
Integrate the marketing landing page as the Next.js home page and wire the lead capture form to Supabase.

Purpose: Replace the placeholder home page with a real marketing page that captures leads into the database, giving the product a public-facing entry point.
Output: Landing page at `/`, lead capture modal, leads API route, marina_leads DDL.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-landing-page-and-marina-onboarding/02-CONTEXT.md
@.planning/phases/02-landing-page-and-marina-onboarding/02-RESEARCH.md

@landing-page/index.html
@landing-page/styles.css
@src/app/layout.tsx
@src/app/page.tsx
@src/lib/supabase/admin.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/lib/supabase/admin.ts:
```typescript
export function createAdminClient(): SupabaseClient<Database>
```

From src/app/layout.tsx:
```typescript
// Root layout - add Font Awesome CDN <link> in <head> here
export default function RootLayout({ children }: { children: React.ReactNode })
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create marina_leads DDL and lead capture API route</name>
  <files>database/003_marina_leads.sql, src/app/api/leads/route.ts, src/__tests__/lead-api.test.ts</files>
  <behavior>
    - POST /api/leads with valid {name, email, user_type} returns 201 with {success: true}
    - POST /api/leads with empty name returns 400 with {errors: {name: "Name is required"}}
    - POST /api/leads with invalid email returns 400 with {errors: {email: "Invalid email"}}
    - POST /api/leads with missing user_type returns 400 with {errors: {user_type: "Please select a user type"}}
    - POST /api/leads with multiple missing fields returns all errors in one response
  </behavior>
  <action>
    1. Create `database/003_marina_leads.sql` with the marina_leads table:
       - id UUID PK default gen_random_uuid()
       - name TEXT NOT NULL
       - email TEXT NOT NULL
       - user_type TEXT NOT NULL CHECK (user_type IN ('yacht_owner', 'marina_owner'))
       - created_at TIMESTAMPTZ DEFAULT NOW()
       - Enable RLS (no policies needed since admin client bypasses RLS)

    2. Create `src/app/api/leads/route.ts`:
       - POST handler that parses JSON body {name, email, user_type}
       - Inline validation: name required (trimmed), email required + regex check, user_type required
       - Return 400 with {errors: Record<string, string>} on validation failure
       - Use `createAdminClient()` from `@/lib/supabase/admin` (not the server client -- admin bypasses RLS)
       - Insert into marina_leads table
       - Return 201 with {success: true} on success
       - Return 500 with {error: "Failed to save"} on DB error

    3. Write tests in `src/__tests__/lead-api.test.ts`:
       - Extract the validation logic into a testable function or test the route handler directly
       - Mock the Supabase admin client
       - Test all behavior cases listed above
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/lead-api.test.ts --reporter=verbose</automated>
  </verify>
  <done>API route validates and inserts leads; all test cases pass; DDL file ready for manual execution in Supabase SQL Editor</done>
</task>

<task type="auto">
  <name>Task 2: Port landing page to Next.js home page with lead modal</name>
  <files>src/app/page.tsx, src/components/lead-modal.tsx, src/app/layout.tsx</files>
  <action>
    1. Add Font Awesome CDN link to `src/app/layout.tsx`:
       - Add `<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />` inside the `<head>` section of the `<html>` element (before `<body>`)

    2. Create `src/components/lead-modal.tsx` ("use client"):
       - Props: `isOpen: boolean`, `onClose: () => void`
       - Modal overlay with centered card (same visual pattern as the static landing page modal)
       - User type selector: two clickable cards (yacht owner with ship icon, marina owner with anchor icon) -- clicking sets user_type state and highlights the selected card
       - Form fields: name (text), email (email) -- simplified per user decision (no message, no launch notify)
       - Inline validation errors displayed below each field (red text, shown on submit attempt)
       - On submit: POST to `/api/leads` with {name, email, user_type}
       - Success state: replace form content with a confirmation message (e.g., "Thanks! We'll be in touch soon.") -- use in-modal confirmation per Claude's discretion
       - Close button (X) in top-right corner
       - Close on overlay click (outside modal)
       - Close on Escape key

    3. Replace `src/app/page.tsx` entirely ("use client"):
       - Hero section: Full-width background (navy gradient or marina photo from unsplash, matching the static page's feel), headline "Find the Perfect Marina in South Florida" (regional per user decision, not "nationwide"), subheadline, two CTA buttons ("Find Marina Space" and "List Your Marina") that both open the lead modal
       - Drop the trust indicators (500+ marinas, 10,000+ yacht owners) -- these are fabricated stats from the static page
       - How It Works section: Two-tab layout (For Yacht Owners / For Marina Owners) matching the static page's content. Each tab has 3 step cards with Font Awesome icons. Use client-side state to toggle tabs.
       - Sections to DROP per user decision: Features, Marina Network, Affiliate Program, Testimonials
       - Footer: Simple footer with EasyDock branding, copyright, disclaimer text (reuse from static page)
       - All styling in Tailwind CSS utility classes -- match navy/teal theme proportions from static page but do not import styles.css
       - Import and render LeadModal with useState for isOpen
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Root URL renders marketing landing page with Hero, How It Works tabs, footer, and functional lead capture modal; no build errors; Font Awesome icons render correctly; visual feel matches the static landing page's navy/teal theme</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- `npx vitest run src/__tests__/lead-api.test.ts` passes all cases
- Landing page renders at `/` with Hero and How It Works sections
- Lead modal opens, validates, and submits to API
</verification>

<success_criteria>
- Root URL shows marketing landing page (not the old placeholder)
- Lead capture form validates inline and submits to `/api/leads`
- marina_leads DDL file ready for SQL Editor execution
- Font Awesome icons render on the landing page
- Build passes with no type errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-landing-page-and-marina-onboarding/02-01-SUMMARY.md`
</output>
