---
phase: 05-booking-lifecycle-and-notifications
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified:
  - src/emails/booking-created.tsx
  - src/emails/booking-approved.tsx
  - src/emails/booking-denied.tsx
  - src/emails/booking-cancelled.tsx
  - src/lib/email/send.ts
  - src/app/api/checkout/route.ts
  - src/app/api/bookings/[id]/approve/route.ts
  - src/app/api/bookings/[id]/deny/route.ts
  - src/app/api/bookings/[id]/cancel/route.ts
  - src/__tests__/email-send.test.ts
autonomous: true
requirements: [EMAL-01, EMAL-02]
user_setup:
  - service: resend
    why: "Transactional email delivery for booking notifications"
    env_vars:
      - name: RESEND_API_KEY
        source: "Resend Dashboard -> API Keys -> Create API Key"
      - name: NEXT_PUBLIC_APP_URL
        source: "Your deployed app URL (e.g., https://easydock.netlify.app) — used in email 'View Booking' links"
    dashboard_config:
      - task: "Verify easydock.com domain for sending from bookings@easydock.com"
        location: "Resend Dashboard -> Domains -> Add Domain -> follow DNS record instructions"

must_haves:
  truths:
    - "Both parties receive email when a booking is created"
    - "Yacht owner receives email when booking is approved"
    - "Yacht owner receives email when booking is denied"
    - "Both parties receive email when booking is cancelled"
    - "Email contains booking details: marina name, slip, dates, price"
    - "Email includes a 'View Booking' button linking to the booking detail page"
    - "Email failure does not prevent the booking action from succeeding"
  artifacts:
    - path: "src/lib/email/send.ts"
      provides: "Resend email wrapper with sendBookingEmail function"
      exports: ["sendBookingEmail"]
    - path: "src/emails/booking-created.tsx"
      provides: "React Email template for booking creation"
    - path: "src/emails/booking-approved.tsx"
      provides: "React Email template for booking approval"
    - path: "src/emails/booking-denied.tsx"
      provides: "React Email template for booking denial"
    - path: "src/emails/booking-cancelled.tsx"
      provides: "React Email template for booking cancellation"
    - path: "src/__tests__/email-send.test.ts"
      provides: "Unit tests for email send helper"
  key_links:
    - from: "src/app/api/checkout/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendBookingEmail('created', ...) after RPC succeeds"
      pattern: "sendBookingEmail.*created"
    - from: "src/app/api/bookings/[id]/approve/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendBookingEmail('approved', ...) after DB update"
      pattern: "sendBookingEmail.*approved"
    - from: "src/app/api/bookings/[id]/cancel/route.ts"
      to: "src/lib/email/send.ts"
      via: "sendBookingEmail('cancelled', ...) after DB update + Stripe refund"
      pattern: "sendBookingEmail.*cancelled"
    - from: "src/lib/email/send.ts"
      to: "resend.emails.send"
      via: "Resend SDK with react property for server-side rendering"
      pattern: "resend\\.emails\\.send"
---

<objective>
Install Resend and React Email, create branded email templates for all booking events, wire email sends into all booking API routes.

Purpose: Both parties must receive email notifications on every booking event (EMAL-01 for creation, EMAL-02 for status changes). Emails are non-fatal — they must not block the booking action if they fail.

Output: 4 React Email templates, Resend send helper, email integration in checkout + approve/deny/cancel routes, unit tests.
</objective>

<execution_context>
@C:/Users/julio/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/julio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-booking-lifecycle-and-notifications/05-CONTEXT.md
@.planning/phases/05-booking-lifecycle-and-notifications/05-RESEARCH.md
@.planning/phases/05-booking-lifecycle-and-notifications/05-01-SUMMARY.md
@.planning/phases/05-booking-lifecycle-and-notifications/05-02-SUMMARY.md

@src/app/api/checkout/route.ts
@src/app/api/bookings/[id]/approve/route.ts
@src/app/api/bookings/[id]/deny/route.ts
@src/app/api/bookings/[id]/cancel/route.ts
@src/lib/supabase/admin.ts
@src/types/database.ts

<interfaces>
<!-- Resend SDK pattern (from research) -->
```typescript
import { Resend } from 'resend';
const resend = new Resend(process.env.RESEND_API_KEY);

const { data, error } = await resend.emails.send({
  from: 'EasyDock <bookings@easydock.com>',
  to: ['user@example.com'],
  subject: 'Subject line',
  react: TemplateComponent({ ...props }),  // Server-side rendered by Resend
});
```

<!-- React Email component imports -->
```typescript
import {
  Body, Container, Head, Heading, Html, Preview,
  Section, Text, Button, Hr
} from '@react-email/components';
```

<!-- Admin client needed to look up emails across profiles (cross-user reads blocked by RLS) -->
```typescript
import { createAdminClient } from "@/lib/supabase/admin";
// Fetch: profiles.email via boat_owner_id and marinas.owner_id -> profiles.email
```

<!-- Checkout route context: booking creation happens via RPC, bookingId is available after line 144 -->
<!-- Approve/deny/cancel routes: booking data with marina join is available after DB operations -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install dependencies, create email templates and send helper with tests</name>
  <files>
    src/emails/booking-created.tsx,
    src/emails/booking-approved.tsx,
    src/emails/booking-denied.tsx,
    src/emails/booking-cancelled.tsx,
    src/lib/email/send.ts,
    src/__tests__/email-send.test.ts
  </files>
  <behavior>
    - Test: sendBookingEmail calls resend.emails.send with correct from address
    - Test: sendBookingEmail('created', ...) sends to BOTH yacht owner and marina owner emails
    - Test: sendBookingEmail('approved', ...) sends to yacht owner email only
    - Test: sendBookingEmail('denied', ...) sends to yacht owner email only
    - Test: sendBookingEmail('cancelled', ...) sends to BOTH parties
    - Test: sendBookingEmail logs error but does not throw when Resend returns an error
    - Test: sendBookingEmail fetches emails via adminClient from profiles table
  </behavior>
  <action>
    1. Run `npm install resend @react-email/components`

    2. Create src/lib/email/send.ts:
       - Initialize Resend with process.env.RESEND_API_KEY
       - Define BookingEmailParams interface: { bookingId, marinaName, slipName, checkIn, checkOut, totalPrice, boatOwnerEmail, marinaOwnerEmail }
       - Export async function sendBookingEmail(trigger: 'created' | 'approved' | 'denied' | 'cancelled', params: BookingEmailParams): Promise<void>
       - Determine recipients by trigger:
         - created: [boatOwnerEmail, marinaOwnerEmail]
         - approved: [boatOwnerEmail]
         - denied: [boatOwnerEmail]
         - cancelled: [boatOwnerEmail, marinaOwnerEmail]
       - Select the correct React Email template component by trigger
       - Call resend.emails.send with { from: 'EasyDock <bookings@easydock.com>', to: recipients, subject: ..., react: TemplateComponent(params) }
       - Wrap in try/catch — console.error on failure, NEVER throw. Email failure must not block the API response.
       - Also export a helper: async function fetchBookingEmailParams(adminClient, bookingId): Promise<BookingEmailParams> that fetches booking + slip name + marina name + boat_owner profile email + marina owner profile email via adminClient joins.

    3. Create 4 React Email templates in src/emails/:
       - All templates share the same layout: EasyDock branded header (navy #1e3a5f), booking details card (teal accent #0d9488 background section), "View Booking" CTA button
       - booking-created.tsx: Subject "New booking at {marinaName}". Shows marina, slip, dates, price. "View Booking" button links to NEXT_PUBLIC_APP_URL/bookings/{bookingId}
       - booking-approved.tsx: Subject "Booking approved — {marinaName}". Confirms approval with details.
       - booking-denied.tsx: Subject "Booking update — {marinaName}". Informs the yacht owner their booking was not approved.
       - booking-cancelled.tsx: Subject "Booking cancelled — {marinaName}". Confirms cancellation and mentions refund will be processed.
       - Each template: export default function with typed props, uses @react-email/components (Html, Head, Preview, Body, Container, Section, Heading, Text, Button, Hr)
       - Style: inline styles (email clients don't support CSS classes). Navy header (#1e3a5f), teal CTA button (#0d9488), white body, light gray (#f5f7fa) background.

    4. Create test file mocking Resend SDK. Verify: correct recipients per trigger, from address, non-throwing on error.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/email-send.test.ts</automated>
  </verify>
  <done>
    Resend and React Email installed. 4 branded email templates created. sendBookingEmail helper sends to correct recipients per trigger type and never throws on failure. All tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Integrate email sends into all booking API routes</name>
  <files>
    src/app/api/checkout/route.ts,
    src/app/api/bookings/[id]/approve/route.ts,
    src/app/api/bookings/[id]/deny/route.ts,
    src/app/api/bookings/[id]/cancel/route.ts
  </files>
  <action>
    Add email notifications to each API route. In every case, the email call comes AFTER the DB operation succeeds and is wrapped so failures are non-fatal.

    1. src/app/api/checkout/route.ts — EMAL-01 (booking created):
       - After the Stripe session is created and bookingId is confirmed (around line 192), add:
       - Import { sendBookingEmail, fetchBookingEmailParams } from '@/lib/email/send'
       - Call fetchBookingEmailParams(adminClient, bookingId) to get all needed data including both parties' emails
       - Call sendBookingEmail('created', params) — do NOT await in a way that blocks the response. Use a fire-and-forget pattern: `sendBookingEmail('created', params).catch(console.error)` or just await it after the response data is ready (before return).
       - The adminClient is already imported in this file.

    2. src/app/api/bookings/[id]/approve/route.ts — EMAL-02 (approved):
       - After the successful DB update, import and call fetchBookingEmailParams + sendBookingEmail('approved', params)
       - Use adminClient (import from @/lib/supabase/admin) for the email params fetch since cross-user profile reads are blocked by RLS
       - Place email call after the DB update check but before the return statement

    3. src/app/api/bookings/[id]/deny/route.ts — EMAL-02 (denied):
       - Same pattern as approve: after DB update succeeds, call sendBookingEmail('denied', params)
       - Use adminClient for email params fetch

    4. src/app/api/bookings/[id]/cancel/route.ts — EMAL-02 (cancelled):
       - After both DB update and Stripe refund succeed, call sendBookingEmail('cancelled', params)
       - The adminClient is already used in this route
       - Place after the Stripe refund block (or after the skip if no payment_intent)

    Pattern for all routes:
    ```typescript
    // After successful DB operation:
    try {
      const emailParams = await fetchBookingEmailParams(adminClient, bookingId);
      await sendBookingEmail('trigger', emailParams);
    } catch (emailErr) {
      console.error('Email notification failed:', emailErr);
      // Non-fatal — do not affect the API response
    }
    ```
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/ 2>&1 | tail -20</automated>
  </verify>
  <done>
    All 4 API routes (checkout, approve, deny, cancel) send email notifications after successful DB operations. Email failures are logged but do not affect API responses. All existing tests still pass, build succeeds.
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/email-send.test.ts` — email helper tests pass
- `npx vitest run src/__tests__/` — full test suite passes (no regressions)
- `npx next build` — no build errors
- Email templates render valid HTML (verified by import + no build errors)
</verification>

<success_criteria>
- Resend and @react-email/components installed
- 4 branded React Email templates with EasyDock navy/teal styling and "View Booking" CTA
- sendBookingEmail helper sends to correct recipients per event type
- Email integrated into checkout (created), approve, deny, and cancel routes
- Email failure never blocks the booking operation
- All tests pass, build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/05-booking-lifecycle-and-notifications/05-03-SUMMARY.md`
</output>
